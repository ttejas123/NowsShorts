import 'package:flutter/material.dart';

class AppColors {
  // ───────────────── Brand ─────────────────
  static const primary = Color(0xFF4A7DFF); // Main blue
  static const primaryDark = Color(0xFF2F5EDB); // Darker blue
  static const success = Color(0xFF2DBE60);
  static const danger = Color(0xFFE53935);

  // ───────────────── Light Theme ─────────────────
  static const lightBackground = Color(0xFFFFFFFF);
  static const lightCard = Color(0xFFF2F2F2);
  static const lightDivider = Color(0xFFE0E0E0);

  static const lightTextPrimary = Color(0xFF1C1C1C);
  static const lightTextSecondary = Color(0xFF6A6A6A);
  static const lightTextHint = Color(0xFF9E9E9E);

  // ───────────────── Dark Theme ─────────────────
  static const darkBackground = Color(0xFF121212);
  static const darkCard = Color(0xFF1E1E1E);
  static const darkDivider = Color(0xFF2A2A2A);

  static const darkTextPrimary = Color(0xFFFFFFFF);
  static const darkTextSecondary = Color(0xFFB0B0B0);
  static const darkTextHint = Color(0xFF7A7A7A);
}
